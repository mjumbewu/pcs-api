{% if session %}
{"session" : {
	"id" : "{{ session.id }}",
	"userid" : "{{ session.user }}",
	"name" : "{{ session.name }}"
}}
{% else %}
{"error" : {
	"msg" : "No session found",
	"code" : ""
}}
{% endif %}